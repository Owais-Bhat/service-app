# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

Pinned to SDK 56 (not the newest 57) deliberately — 57 was ~3 weeks old
when this was set up and Expo Go's public app-store release hadn't caught
up yet, causing "Project is incompatible with this version of Expo Go" on
real devices. Before bumping the SDK again, confirm Expo Go has actually
shipped support for the target version.
