# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

Bumped from SDK 56 to 57.0.9 on 2026-08-19 — the device's Expo Go had
auto-updated to a version aligned with SDK 57, which no longer accepted
the pinned SDK 56 project (manifest loaded but the bundle transfer hung
indefinitely on-device, on LAN and tunnel connections alike). Before
bumping the SDK again, confirm Expo Go has actually shipped support for
the target version.
